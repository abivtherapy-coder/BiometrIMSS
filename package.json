{
  "name": "biometrimss",
  "version": "2.0.0",
  "private": true,
  "description": "Control personal de guardias y registros biométricos.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check logic.js && node --check app.js && node --check service-worker.js",
    "serve": "npx --yes serve ."
  },
  "engines": {
    "node": ">=18"
  }
}
